name: Build and Push to Private Docker Hub

on:
  push:
    branches:
      - layout 

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker image
        run: - name: Build Docker image
  run: docker build -t ${{ secrets.DOCKER_USERNAME }}/documentmanager-web:latest -f DocumentManager/Dockerfile .


      - name: Push Docker image
        run: docker push chitwanrana/documentmanager-web:latest
